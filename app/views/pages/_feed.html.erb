<div class="maincard">
  <h3>Ses derniers posts Instagram</h3>
<%# raw(ap @image) %>
  <div class="mr-2">
    <% @images.each do |image|  %>
      <%= image_tag image, width: 100, height: 100 %>
    <% end %>
  </div>
</div>
