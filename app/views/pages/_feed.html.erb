<div class="maincard">
  <h5>Dernier post INSTAGRAM</h5>
<%# raw(ap @image) %>
  <div class="mr-2">
    <% @images.each do |image|  %>
      <%= image_tag image, width: 100, height: 100 %>
    <% end %>
  </div>
</div>
